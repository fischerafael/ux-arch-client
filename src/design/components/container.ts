import styled from 'styled-components'
import { LayoutFlex } from './layout'

export const ContainerDashboardMainSection = styled(LayoutFlex)`
    height: 80vh;
    align-items: flex-start;
    padding: 1rem;
`

export const ContainerBlogMainSection = styled(LayoutFlex)`
    height: 80vh;
    align-items: flex-start;
    padding: 1rem;
`
